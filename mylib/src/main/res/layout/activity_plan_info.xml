<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.plan.PlanInfoActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/em_light_grey">

        <TextView
            android:id="@+id/activity_plan_info_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingBottom="@dimen/spacing_standard"
            android:paddingLeft="@dimen/spacing_half"
            android:paddingRight="@dimen/spacing_half"
            android:paddingTop="@dimen/spacing_standard"
            android:text="@string/erplan_info_message"
            android:textSize="@dimen/font_small"
            android:textColor="@color/em_black" />

        <ProgressBar
            android:id="@+id/activity_plan_info_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/activity_plan_info_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/send_acknowledgement_button"
            android:layout_below="@+id/activity_plan_info_message">
        </androidx.recyclerview.widget.RecyclerView>

        <Button
            android:id="@+id/send_acknowledgement_button"
            android:text="@string/send_acknowledgement"
            android:textSize="@dimen/font_med"
            android:textColor="@color/white"
            android:background="@color/black"
            android:layout_margin="@dimen/spacing_half"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:padding="@dimen/spacing_half"
            android:visibility="gone"/>

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>